#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../typescript-tools/bin/tss" "$@"
  ret=$?
else 
  node  "$basedir/../typescript-tools/bin/tss" "$@"
  ret=$?
fi
exit $ret
